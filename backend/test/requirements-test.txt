# requirements-test.txt
# Testing dependencies for the semantic search project

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0          # Coverage reporting
pytest-mock>=3.11.1        # Enhanced mocking capabilities

# Code quality
ruff>=0.8.0                # Linting + formatting
pyright>=1.1.390           # Type checking

# Optional: for better test output
pytest-sugar>=0.9.7        # Enhanced test output
pytest-xdist>=3.3.1        # Parallel test execution

# Reporting — used to generate the dissertation PDF
pytest-html>=4.1.1         # Colour-coded HTML report (open in browser → print to PDF)
pytest-json-report>=1.5.0  # Machine-readable JSON output for custom analysis
