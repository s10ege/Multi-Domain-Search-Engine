# Core
txtai>=7.0.0
pandas>=2.0.0

# Needed by txtai embeddings (model download + transformer runtime)
sentence-transformers>=2.6.0
transformers>=4.40.0
torch>=2.1.0

# Hugging Face hub (for downloading/caching models)
huggingface-hub>=0.20.0

# Optional but recommended (often used by txtai stack)
numpy>=1.24.0
scikit-learn>=1.3.0

faiss-cpu>=1.7.4; platform_system != "Windows"

